def country = "nigera"

pipeline {
	agent any
        stages { 
	    stage('build') {
                   steps {
	             sh "echo ${country}" || sh 'echo lorence'
                }                 
            }
    }        
}

