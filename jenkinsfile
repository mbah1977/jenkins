def country = "nigera"

pipeline {
	agent {
           node { label 'master'}
        }
        parameters {
           string(name:'WHO', defaultValue: 'lorence', description: 'nothing')
        }
        stages { 
	    stage('build') {
                   when {
                        currentBuild.result == 'successful'
                   }
                   input {
                      message " Should we"
                      ok" yes"
                   }
                   steps {
	             sh "echo ${country}" 
                     echo "${params.WHO}"
                }                 
            }
       }
       post {
           failure {
                   sh 'it failed'
	}
    }        
}

