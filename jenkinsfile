
pipeline {
	agent any
        
        def country = 'nigeria'
	stages	{
		stage('build') {
                    parameters {
                             
			steps {
				//sh 'ansible-playbook yaml.yaml -i inventory'
                                 sh "echo ${country}"
			}
		}
	}
}
