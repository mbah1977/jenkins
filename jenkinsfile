def country = "nigera"

pipeline {
	agent any
        stages { 
	    stage('build') {
                   steps {
	             sh "echo ${country}"
                }                 
            }
        }
        post {
             failure {
                 sh 'touch lorence'
              }
        }     
}

