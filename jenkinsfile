def country = "nigera"

pipeline {
	agent {
           node { label 'master'}
        }
        parameters {
           string(name:'WHO', defaultValue: 'lorence', description: 'nothing')
        }
        stages { 
	    stage('build') {
                   when {
                        branch 'master'
                   }
                   input {
                      message " Should we"
                      ok" yes"
                   }
                   steps {
	             sh "echo ${country}" 
                     echo "${params.WHO}"
                }                 
            }
       }
       post {
           failure {
                   sh 'it failed'
	}
    }        
}

