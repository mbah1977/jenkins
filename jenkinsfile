def country = "nigera"

pipeline {
	agent any
        stages { 
	    stage('build') {
                   steps {
	             sh "echo ${country}"
                }                 
            }
        
                   post {
                       always {
                          sh 'touch lorence'
              }
    }      
  }     
}

