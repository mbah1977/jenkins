def country = "nigera"

pipeline {
	agent any
        stages { 
	    stage('build') {
                             
			steps {
				//sh 'ansible-playbook yaml.yaml -i inventory'
                                 sh "echo ${country}"
                                 
			}
		        post {
                             failure {
                                       sh 'touch lorence'

                 }
              }
            }
	}
}

