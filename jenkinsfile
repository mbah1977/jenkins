def country = "nigera"

pipeline {
	agent any
        stages { 
	    stage('build') {
                   input {
                      message " Should we"
                      ok" yes"
                   }
                   steps {
	             sh "echo ${country}" 
                }                 
            }
    }        
}

